pipeline {
    agent any

    stages {
        stage('ONE') {
            steps {
                sh 'cd Sample_Jenkins'
                echo 'Testing..'
                sh 'hostname'
            }
        }
    }
}